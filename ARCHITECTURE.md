# 🏗️ System Architecture

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                    (React + TypeScript)                         │
├─────────────────────────────────────────────────────────────────┤
│  Home  │ Assessment │ Actions │ Footprint │ Predictions │ Dashboard │
└────┬────────────────────────────────────────────────────────┬───┘
     │                                                        │
     │                  HTTPS / REST API                      │
     │                                                        │
┌────▼────────────────────────────────────────────────────────▼───┐
│                      API GATEWAY (FastAPI)                      │
│                    http://localhost:8000/api                    │
└────┬──────┬──────┬──────┬──────┬──────────────────────────────┘
     │      │      │      │      │
     │      │      │      │      │
┌────▼──┐ ┌─▼───┐ ┌▼────┐┌▼────┐┌▼─────────┐
│ Risk  │ │Act- │ │Clim-││Foot-││Predict-  │
│Routes │ │ion  │ │ate  ││print││ion       │
│       │ │Route│ │Route││Route││Routes    │
└───┬───┘ └──┬──┘ └─┬───┘└─┬───┘└────┬─────┘
    │        │      │      │         │
    └────────┴──────┴──────┴─────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
    ┌───▼────┐          ┌─────▼─────┐
    │   AI   │          │  External │
    │ Models │          │    APIs   │
    ├────────┤          ├───────────┤
    │ Risk   │          │OpenWeather│
    │Action  │          │  NASA     │
    │Predict │          │  Geopy    │
    └───┬────┘          └───────────┘
        │
    ┌───▼────────┐
    │  Database  │
    │  (SQLite)  │
    └────────────┘
```

## Component Architecture

### Frontend Layer
```
┌─────────────────────────────────────────────────────┐
│                    React App                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │  Pages   │  │Components│  │ Services │        │
│  ├──────────┤  ├──────────┤  ├──────────┤        │
│  │ Home     │  │ Navbar   │  │ API      │        │
│  │ Assess   │  │ Cards    │  │ Client   │        │
│  │ Actions  │  │ Charts   │  └──────────┘        │
│  │ Footprint│  │ Maps     │                       │
│  │ Predict  │  │ Forms    │  ┌──────────┐        │
│  │ Dashboard│  └──────────┘  │  Store   │        │
│  └──────────┘                │ (Zustand)│        │
│                               └──────────┘        │
│                                                     │
│  ┌─────────────────────────────────────────┐      │
│  │         Styling & UI                    │      │
│  ├─────────────────────────────────────────┤      │
│  │ TailwindCSS │ Framer Motion │ Chart.js │      │
│  └─────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

### Backend Layer
```
┌─────────────────────────────────────────────────────┐
│                   FastAPI App                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │              Route Handlers                │   │
│  ├────────────────────────────────────────────┤   │
│  │ /risk/assess                              │   │
│  │ /actions/generate                         │   │
│  │ /climate/current                          │   │
│  │ /footprint/calculate                      │   │
│  │ /predictions/generate                     │   │
│  └───────────┬────────────────────────────────┘   │
│              │                                     │
│  ┌───────────▼────────────┐  ┌──────────────┐    │
│  │     AI Models          │  │   Services   │    │
│  ├────────────────────────┤  ├──────────────┤    │
│  │ • RiskAssessmentAI     │  │ • Database   │    │
│  │   - Flood              │  │ • Climate    │    │
│  │   - Wildfire           │  │ • Geocoding  │    │
│  │   - Hurricane          │  └──────────────┘    │
│  │   - Drought            │                       │
│  │   - Heatwave           │  ┌──────────────┐    │
│  │   - Sea Level          │  │   Models     │    │
│  │                        │  ├──────────────┤    │
│  │ • ActionPlannerAI      │  │ • Risk       │    │
│  │   - Priority Matrix    │  │ • Action     │    │
│  │   - Cost Optimization  │  │ • Climate    │    │
│  │   - Timeline           │  │ • Footprint  │    │
│  │                        │  └──────────────┘    │
│  │ • PredictionEngine     │                       │
│  │   - Time Series        │                       │
│  │   - Scenarios          │                       │
│  │   - Trends             │                       │
│  └────────────────────────┘                       │
└─────────────────────────────────────────────────────┘
```

## Data Flow

### Risk Assessment Flow
```
User Input (Location)
       │
       ▼
  Geocoding (Geopy)
       │
       ▼
  Get Coordinates (lat, lon)
       │
       ▼
  Fetch Climate Data
  ┌─────────────────────┐
  │ • Current Weather   │
  │ • Historical Data   │
  │ • Trends            │
  └──────────┬──────────┘
             │
             ▼
    AI Risk Assessment
    ┌─────────────────────┐
    │ Analyze 6 Factors:  │
    │ 1. Flood            │
    │ 2. Wildfire         │
    │ 3. Hurricane        │
    │ 4. Drought          │
    │ 5. Heatwave         │
    │ 6. Sea Level Rise   │
    └──────────┬──────────┘
               │
               ▼
      Calculate Scores
      ┌──────────────┐
      │ • Individual │
      │ • Overall    │
      │ • Confidence │
      └──────┬───────┘
             │
             ▼
      Save to Database
             │
             ▼
      Return to Frontend
             │
             ▼
      Display Results
```

### Action Plan Flow
```
Risk Assessment Results
         │
         ▼
  Extract Top Risks
         │
         ▼
  AI Action Planner
  ┌─────────────────────┐
  │ For each risk:      │
  │ • Query templates   │
  │ • Filter by score   │
  │ • Add general items │
  └──────────┬──────────┘
             │
             ▼
   Prioritize Actions
   ┌──────────────────┐
   │ • By priority    │
   │ • By impact      │
   │ • By timeframe   │
   └────────┬─────────┘
            │
            ▼
   Calculate Metrics
   ┌──────────────────┐
   │ • Total cost     │
   │ • Avg impact     │
   │ • Timeline       │
   └────────┬─────────┘
            │
            ▼
   Save to Database
            │
            ▼
   Return to Frontend
            │
            ▼
   Display Plan
```

## Database Schema

```sql
┌─────────────────────────────────────────┐
│         risk_assessments                │
├─────────────────────────────────────────┤
│ id              INTEGER PRIMARY KEY     │
│ location        STRING                  │
│ latitude        FLOAT                   │
│ longitude       FLOAT                   │
│ risk_score      FLOAT                   │
│ risk_level      STRING                  │
│ risk_types      JSON                    │
│ assessment_data JSON                    │
│ created_at      DATETIME                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│           action_plans                  │
├─────────────────────────────────────────┤
│ id              INTEGER PRIMARY KEY     │
│ user_id         STRING                  │
│ location        STRING                  │
│ risk_types      JSON                    │
│ actions         JSON                    │
│ priority        STRING                  │
│ estimated_cost  FLOAT                   │
│ estimated_impact FLOAT                  │
│ created_at      DATETIME                │
│ updated_at      DATETIME                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│          climate_data                   │
├─────────────────────────────────────────┤
│ id              INTEGER PRIMARY KEY     │
│ location        STRING                  │
│ latitude        FLOAT                   │
│ longitude       FLOAT                   │
│ temperature     FLOAT                   │
│ precipitation   FLOAT                   │
│ humidity        FLOAT                   │
│ wind_speed      FLOAT                   │
│ data_source     STRING                  │
│ raw_data        JSON                    │
│ timestamp       DATETIME                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│        carbon_footprints                │
├─────────────────────────────────────────┤
│ id              INTEGER PRIMARY KEY     │
│ user_id         STRING                  │
│ category        STRING                  │
│ activity_type   STRING                  │
│ amount          FLOAT                   │
│ emissions_kg    FLOAT                   │
│ calculation_data JSON                   │
│ created_at      DATETIME                │
└─────────────────────────────────────────┘
```

## Technology Stack Details

### Frontend Stack
```
React 18.2.0
  ├── TypeScript 5.3.3
  ├── Vite 5.0.11 (Build Tool)
  ├── React Router 6.21.0 (Navigation)
  ├── TailwindCSS 3.4.1 (Styling)
  ├── Framer Motion 10.18.0 (Animations)
  ├── Chart.js 4.4.1 (Charts)
  ├── React-Chartjs-2 5.2.0
  ├── Leaflet 1.9.4 (Maps)
  ├── React-Leaflet 4.2.1
  ├── Axios 1.6.5 (HTTP Client)
  ├── Zustand 4.4.7 (State)
  └── React Hot Toast 2.4.1 (Notifications)
```

### Backend Stack
```
Python 3.9+
  ├── FastAPI 0.109.0 (Framework)
  ├── Uvicorn 0.27.0 (Server)
  ├── SQLAlchemy 2.0.25 (ORM)
  ├── Pydantic 2.5.3 (Validation)
  ├── Geopy 2.4.1 (Geocoding)
  ├── NumPy 1.26.3 (Computing)
  ├── Pandas 2.2.0 (Data)
  └── Httpx 0.26.0 (HTTP Client)
```

## Deployment Architecture

### Docker Deployment
```
┌─────────────────────────────────────────┐
│         Docker Compose                  │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────┐  ┌──────────────┐   │
│  │   Backend    │  │   Frontend   │   │
│  │  Container   │  │  Container   │   │
│  ├──────────────┤  ├──────────────┤   │
│  │ Python 3.11  │  │  Node 18     │   │
│  │ FastAPI      │  │  Nginx       │   │
│  │ Port: 8000   │  │  Port: 80    │   │
│  └──────┬───────┘  └──────┬───────┘   │
│         │                  │            │
│         └──────────┬───────┘            │
│                    │                    │
│         ┌──────────▼─────────┐         │
│         │  Network Bridge    │         │
│         └────────────────────┘         │
└─────────────────────────────────────────┘
```

### Cloud Deployment Options
```
┌─────────────────────────────────────────┐
│           Deployment Options            │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────┐  ┌──────────────┐   │
│  │   Heroku     │  │   Vercel     │   │
│  │   (Backend)  │  │  (Frontend)  │   │
│  └──────────────┘  └──────────────┘   │
│                                         │
│  ┌──────────────┐  ┌──────────────┐   │
│  │     AWS      │  │    Azure     │   │
│  │ ECS/Fargate  │  │  App Service │   │
│  └──────────────┘  └──────────────┘   │
│                                         │
│  ┌──────────────┐  ┌──────────────┐   │
│  │     GCP      │  │   Netlify    │   │
│  │  Cloud Run   │  │  (Frontend)  │   │
│  └──────────────┘  └──────────────┘   │
└─────────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────┐
│          Security Layers                │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────────────────────────┐  │
│  │     HTTPS / TLS Encryption       │  │
│  └──────────────┬───────────────────┘  │
│                 │                       │
│  ┌──────────────▼───────────────────┐  │
│  │      CORS Configuration          │  │
│  │   (Origin Validation)            │  │
│  └──────────────┬───────────────────┘  │
│                 │                       │
│  ┌──────────────▼───────────────────┐  │
│  │    Input Validation              │  │
│  │   (Pydantic Models)              │  │
│  └──────────────┬───────────────────┘  │
│                 │                       │
│  ┌──────────────▼───────────────────┐  │
│  │    SQL Injection Prevention      │  │
│  │      (ORM Queries)               │  │
│  └──────────────┬───────────────────┘  │
│                 │                       │
│  ┌──────────────▼───────────────────┐  │
│  │   Environment Variables          │  │
│  │    (Secret Management)           │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## Performance Optimization

```
Frontend Optimizations:
├── Code Splitting (Vite)
├── Lazy Loading (React.lazy)
├── Image Optimization
├── CSS Purging (TailwindCSS)
├── Compression (gzip)
└── Caching Strategy

Backend Optimizations:
├── Async/Await Operations
├── Database Connection Pooling
├── Response Caching
├── Query Optimization
├── Pagination Support
└── Compression Middleware
```

This architecture provides:
✅ Scalability
✅ Maintainability
✅ Security
✅ Performance
✅ Flexibility
✅ Extensibility
